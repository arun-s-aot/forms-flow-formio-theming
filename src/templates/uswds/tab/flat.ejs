{% ctx.component.components.forEach(function(tab, index) { %}
  <div class="margin-bottom-2 formio-formsflow-card">
    <div class="formio-formsflow-card-header bg-base-lightest">
      <h4 class="margin-bottom-0 formio-formsflow-card-title">{{ ctx.t(tab.label) }}</h4>
    </div>
    <div class="formio-formsflow-card-body" ref="{{ctx.tabKey}}">
      {{ ctx.tabComponents[index] }}
    </div>
  </div>
{% }) %}

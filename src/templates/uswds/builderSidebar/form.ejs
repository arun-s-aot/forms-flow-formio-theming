<div id="{{ctx.groupId}}" class="formsflow-accordion formsflow-accordion--bordered builder-sidebar{{ctx.scrollEnabled ? ' builder-sidebar_scroll' : ''}}" ref="sidebar">
  {% ctx.groups.forEach(function(group) { %}
    {{ group }}
  {% }) %}
</div>
